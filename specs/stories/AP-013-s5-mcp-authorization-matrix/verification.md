# WI-014 authorization matrix observation — 2026-09-15 13:17 UTC

This records local observations of the AP-013 working-tree content based on
`fdc6fb7853008ccfe22462a6f2b7198c125befd6`. The candidate includes the
uncommitted AP-011/AP-012 material already present in the working tree. The
`WI-014 --snapshot` table label names the candidate-binding method; it does
not assert that an immutable snapshot already existed at the observation
time. Exact snapshot identity and canonical gate log belong to ForgePilot;
query ForgePilot for current Work Item, Gate, verification or review state.
This record does not project any of those states.

Source and fixture: macOS 26.5.1, Node 24.21.0, pnpm 12.4.1,
`tests/acceptance/s5-mcp-authorization-matrix.test.ts` using the official MCP
Client, mutable Registry and real SQLite; `tests/integration/registry-revision-fence.test.ts`
uses the same SQLite commit barrier. Values are bounded synthetic identifiers.
All ten rows below refer to the same working-tree content at observation.
`inspectDurability` is the result location for receipt, Context, binding
snapshot, reservation and receipt-actor counts; `inspectProductAudit` is the
result location for persisted Principal/tool/outcome metadata. The fixtures
assert each observation rather than logging private request arguments.

| Candidate           | Existing tool                        | Fixture / command                                                                                        | Expected result                                                                              | Actual result                                                                                                                                                           | Persistent state / Runtime starts                                                                                                                                   | Audit result location                                                    |
| ------------------- | ------------------------------------ | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `WI-014 --snapshot` | `agentport_list_agents`              | `same-Scope, allowlist, inactive Principal, unknown cursor; test:mcp`                                    | Scope membership and current allowlist filter Agents; inactive member denied                 | shared actor saw permitted Agents; reduced allowlist showed only retained Agent; inactive `access_denied`; unknown cursor `not_found`                                   | rejected reads changed no receipt/Context/binding/reservation counts; no new start                                                                                  | `inspectProductAudit`: actual shared Principal, `ok` and `access_denied` |
| `WI-014 --snapshot` | `agentport_submit_task`              | `same-Scope submit, foreign Agent/Context, allowlist and revoke; test:mcp`                               | permitted handoff admits once; foreign/revoked cannot create Task or receipt                 | shared actor admitted an independent Task; foreign Agent/Context and removed allowlist `not_found`; inactive `access_denied`                                            | permitted receipt records shared actor; denied calls left all `inspectDurability` counts unchanged; no new start                                                    | `inspectProductAudit`: shared `ok`, bounded `not_found`/`access_denied`  |
| `WI-014 --snapshot` | `agentport_edit_task`                | `same-Scope edit, foreign/revoke, COMMIT barrier; test:mcp + registry-revision-fence`                    | permitted revision advances; foreign or revoked edit cannot commit                           | shared actor advanced revision to 2; foreign/allowlist `not_found`; inactive `access_denied`; pre-COMMIT revocation rejected                                            | denied counts unchanged; race left original instruction/revision and no receipt; explicit same-ID retry was first success                                           | `inspectProductAudit`: shared `ok`, bounded rejection codes              |
| `WI-014 --snapshot` | `agentport_reply`                    | `same-Scope first answer, foreign/revoke, COMMIT barrier; test:mcp + registry-revision-fence`            | authorized first answer persists once; unauthorized answer has no receipt or delivery        | shared actor accepted one answer; foreign/allowlist `not_found`; inactive `access_denied`; revocation barrier rolled back                                               | authorized Task snapshot retained protected answer; denied counts unchanged; race kept Question pending/answer null and same-ID retry first succeeded; no new start | `inspectProductAudit`: shared `ok`, bounded rejection codes              |
| `WI-014 --snapshot` | `agentport_resume_context`           | `unblocked Context, foreign/revoke, COMMIT barrier; test:mcp + registry-revision-fence`                  | current auth checked before business rule; revoked resume cannot unblock                     | authorized unblocked Context returned `invalid_state`; foreign/allowlist `not_found`; inactive `access_denied`; race rejected                                           | denied counts unchanged; race kept successor paused/blocker intact and no receipt; same-ID retry after reauthorization first resumed                                | `inspectProductAudit`: shared actor and bounded outcome code             |
| `WI-014 --snapshot` | `agentport_acknowledge_interruption` | `unconfirmed stop, foreign/revoke, COMMIT barrier; test:mcp + registry-revision-fence`                   | current auth checked; no acknowledgement before stop or after revocation                     | authorized unconfirmed stop returned `not_found`; foreign/allowlist `not_found`; inactive `access_denied`; race rejected                                                | denied counts unchanged; race left Task recovering/no receipt; same-ID retry after reauthorization first interrupted                                                | `inspectProductAudit`: shared actor and bounded outcome code             |
| `WI-014 --snapshot` | `agentport_get_task`                 | `same-Scope, foreign/unknown, inactive member, failed Task; test:mcp`                                    | authorized protected snapshot; foreign/unknown indistinguishable; failed Task query succeeds | shared actor saw existing instruction and accepted Question answer; foreign and unknown both `not_found`; inactive `access_denied`; failed Task returned `ok`/`failed`  | read changed no receipt counts; foreign Task/Question remained unchanged; no new start                                                                              | `inspectProductAudit`: shared `ok`, bounded `not_found`/`access_denied`  |
| `WI-014 --snapshot` | `agentport_list_tasks`               | `same-Scope, foreign Agent/cursor, allowlist, inactive member; test:mcp`                                 | list filters current membership/allowlist and rejects foreign cursor/filter                  | shared actor listed permitted Task; foreign Agent/cursor and removed allowlist `not_found`; inactive `access_denied`                                                    | rejected reads changed no receipt counts; foreign Task list remained protected; no new start                                                                        | `inspectProductAudit`: shared `ok`, bounded rejection codes              |
| `WI-014 --snapshot` | `agentport_get_events`               | `same-Scope after Question answer, foreign Task/cursor, revoke; test:mcp`                                | authorized bounded events omit private prompt/Question/answer; foreign events unavailable    | post-answer event projection contained none of the three synthetic private markers; foreign Task/cursor and removed allowlist `not_found`; inactive `access_denied`     | rejected reads changed no receipt counts; Question answer remained protected in authorized Task snapshot; no new start                                              | `inspectProductAudit`: shared `ok`, bounded rejection codes              |
| `WI-014 --snapshot` | `agentport_cancel_task`              | `same-Scope cancel, foreign/revoke, worker block and replay barrier; test:mcp + registry-revision-fence` | permitted cancellation commits; foreign/revoked cannot change Task                           | shared actor canceled an independent queued Task; foreign/allowlist `not_found`; inactive `access_denied`; blocked mutation rolled back and replayed receipt was fenced | permitted Task became canceled; denied counts unchanged; blocked mutation retained original Task; revocation prevented replay of an already accepted receipt        | `inspectProductAudit`: shared `ok`, bounded rejection codes              |

The pre-dispatch sibling fixture
`tests/integration/s3-dispatch-transaction.test.ts` rejects a Task whose
creator loses current membership or whose Agent binding changes: no Supervisor
start call or new Execution is recorded. Protocol fixture
`tests/acceptance/authorization.test.ts` and the AP-013 matrix record missing or
invalid bearer as HTTP 401, unknown tool/schema rejection without a Task,
stable operation conflict, authorized failed-Task query and sanitized audit and
log sinks. GATE-034's human-selected rule preserves existing protected
instruction, Question schema and answer fields in authorized Task snapshots;
unauthorized responses, errors, events, audit and logs omit private markers.

First failed observations are retained separately from later passes:

| Command and time (UTC)                                            | Expected                                              | Actual                                                                                                                                                                        | Later observation / result location                                                                                                                                |
| ----------------------------------------------------------------- | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Focused AP-013 matrix, 2026-09-15 13:03                           | fixture reaches live Question                         | fixture's same-Context follow-up/cancel made original Task an ineligible queue head; 1/1 failed                                                                               | independent Context setup corrected fixture; focused matrix 3/3 passed at 13:16; local Vitest output and test file                                                 |
| `pnpm run test:faults` parallel with `test:mcp`, 2026-09-15 13:12 | 107 fault tests pass                                  | 106/107 passed; an existing dispatch-time storage read-timeout test hit `storage request timed out` under parallel load                                                       | isolated exact test passed and sequential `test:faults` 107/107 passed at 13:14; local Vitest output; no product code was changed for this timing observation      |
| Focused AP-013 matrix assertion, 2026-09-15 13:16                 | unblocked Context projects its existing business code | fixture expected `not_found`; actual existing code was `invalid_state`; 2/3 passed                                                                                            | assertion corrected to approved current code; focused matrix 3/3 passed at 13:16; local Vitest output and test file                                                |
| `make verify`, 2026-09-15 13:19                                   | 380 tests pass                                        | 379/380 passed, 37 skipped; the existing storage-timeout fixture's second rejection returned `observation_unavailable` instead of its fixed `storage_unavailable` expectation | fixture now waits for worker responsiveness before retry and requires `storage_unavailable`; focused exact test passed at 13:25; local Vitest output and test file |
| `make verify`, 2026-09-15 13:24                                   | lint and test gate pass                               | lint stopped at an untyped matcher in the interim fixture revision; no tests ran                                                                                              | typed exact-code assertion replaced matcher; focused ESLint and exact test passed at 13:25; local command output                                                   |

This candidate has no designated Linux Runtime-account, protected credential,
external HTTPS or trusted Stop Evidence acceptance observation. Synthetic
platform-neutral stop fixtures do not establish Linux Stop Evidence. It does
not close issue 20, issue 21, G5, S6 or G6.
