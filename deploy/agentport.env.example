# AgentPort caller token 範例檔。
#
# 對應 agentport.toml 裡每個 `[[callers]]` 的 `token_env`；token 只從環境變數讀，
# 不寫進 TOML。實際部署時複製這份檔案到 ~/.config/agentport/agentport.env，
# 填入真實 token，並確保 mode 0600（見 README.md「設定」一節）。
#
# 這份 .example 檔本身只是文件，不含真實憑證，可以進版控。

AGENTPORT_TOKEN_GROK=change-me
